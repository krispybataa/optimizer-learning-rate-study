# Optimizer and Learning Rate Study of Ovarian Cancer Prediction

**Course:** HI 192 — Knowledge Representation and Health Decision Support  
**Authors:** DelaCruz, James Angelo · Doria, Marc Jacob · Rodriguez, Augustus Clark Raphael  
**A.Y.:** 2025–2026

---

## Project Overview

This study systematically evaluates **72 deep learning model configurations** for binary
classification of ovarian cancer from histopathological images (cancer / no_cancer).

The experiment matrix is:

| Dimension | Options | Count |
|-----------|---------|------:|
| Base architectures | VGG19, EfficientNetB3, ResNet50, DenseNet121 | 4 |
| Optimizers | Adam, Adagrad, Adamax, AdaDelta, SGD, RMSProp | 6 |
| Learning rates | 0.0001, 0.00001, 0.000001 | 3 |
| **Total runs** | 4 × 6 × 3 | **72** |

**Fixed hyperparameters:** dropout (0.6 / 0.4 / 0.3), dense nodes (256 / 128 / 64),
epochs 50, input 256×256, batch size 32.

**Dataset:** STRAMPN Histopathological Images for Ovarian Cancer Prediction — 987 images.

---

## Folder Structure

```
optimizer-learning-rate-study/
├── dataset/
│   ├── raw/                   ← Place downloaded STRAMPN images here (see below)
│   └── processed/             ← Auto-generated by preprocessing.py
│       ├── train/ (cancer/, no_cancer/)
│       ├── val/   (cancer/, no_cancer/)
│       └── test/  (cancer/, no_cancer/)
│
├── src/
│   ├── preprocessing.py       ← Image loading, resizing, augmentation, split
│   ├── model_builder.py       ← Builds Keras transfer-learning models
│   ├── train.py               ← Single-run training loop
│   ├── evaluate.py            ← Metrics, curves, confusion matrix per run
│   ├── visualize.py           ← Plotting utilities and aggregate figures
│   └── run_all.py             ← Orchestrates all 72 simulation runs
│
├── results/
│   ├── metrics/               ← Per-run CSV files (accuracy, AUC-ROC, …)
│   ├── curves/
│   │   ├── accuracy_loss/     ← Training/validation accuracy & loss plots
│   │   └── auc_roc/           ← AUC-ROC curves per run
│   ├── confusion_matrices/    ← Confusion matrix plots per run
│   └── summary/               ← Aggregate comparison tables and heatmaps
│
├── docs/
│   ├── latex/
│   │   ├── main.tex           ← Master LaTeX document
│   │   ├── sections/          ← Eight section .tex files (01–08)
│   │   ├── figures/           ← Static figures referenced in the paper
│   │   └── tables/            ← Auto-generated LaTeX table files
│   └── pipeline_diagram/      ← Source files for pipeline visualization
│
├── notebooks/
│   ├── 00_data_exploration.ipynb       ← EDA: class distribution, sample images, pixel stats
│   ├── 01_preprocessing_pipeline.ipynb ← Documents every preprocessing step with rationale
│   ├── 02_experiments.ipynb            ← Master notebook: runs all 84 simulations
│   └── 03_results_analysis.ipynb       ← Heatmaps, rankings, discussion prompts
│
├── requirements.txt
├── .gitignore
└── README.md
```

---

## Dataset

This project uses the **STRAMPN Histopathological Images for Ovarian Cancer Prediction** dataset, available from IEEE DataPort:

https://ieee-dataport.org/documents/strampn-histopathological-images-ovarian-cancer-prediction

The dataset is **not included in this repository**. To reproduce this study:
1. Download the dataset from the link above (IEEE account required)
2. Place the contents at `dataset/raw/STRAMPN Dataset/Dataset/`
3. Run `src/preprocessing.py` to generate the processed train/val/test splits

All rights to the dataset belong to its original authors.

---

## Dataset Setup

The STRAMPN dataset is already downloaded and present at `dataset/raw/STRAMPN Dataset/`.

**Actual structure on disk:**
```
dataset/raw/STRAMPN Dataset/Dataset/
├── Ovarian_Cancer/        ← 481 images  (cancer class)
└── Ovarian_Non_Cancer/    ← 506 images  (no_cancer class)
                               ─────────
                               987 images total
```

When running `01_preprocessing_pipeline.ipynb`, update `RAW_DIR` and the class folder
names to match this structure:

```python
RAW_DIR = pathlib.Path('../dataset/raw/STRAMPN Dataset/Dataset')
CLASSES = {
    'cancer':    'Ovarian_Cancer',
    'no_cancer': 'Ovarian_Non_Cancer',
}
```

The notebook will copy the split images into `dataset/processed/train/`, `val/`, and
`test/` under the normalised `cancer/` and `no_cancer/` subfolder names used by all
downstream generators.

> **Note:** `dataset/raw/` and `dataset/processed/` are excluded from version control
> via `.gitignore`. Do **not** commit raw images to the repository.

---

## Running the Full Experiment

```bash
# Install dependencies
pip install -r requirements.txt

# Preprocess the dataset (run once)
python src/preprocessing.py

# Launch all 72 simulation runs
python src/run_all.py
```

Results will be written to the `results/` directory and summary figures to `results/summary/`.

---

## Compiling the LaTeX Report

```bash
cd docs/latex
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

---

## License

The source code in this repository is licensed under the MIT License — see `LICENSE` for details.
The dataset is not covered by this license and remains the property of its original authors.
