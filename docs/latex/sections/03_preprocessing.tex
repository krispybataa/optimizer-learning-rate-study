\section{Preprocessing}
\label{sec:preprocessing}

We handled all our preprocessing using a custom script, \texttt{src/preprocessing.py}. We used standard libraries like \texttt{pathlib} and \texttt{sklearn}, keeping this step separate from the training loop to ensure the data preparation was clear and repeatable.

\subsection{Raw Data Inventory}

First, we took an inventory of the raw dataset to make sure all files were valid JPEGs and that we had the expected counts for each class. The script confirmed that we had 481 cancer images and 506 non-cancer images, all in the correct format.

\begin{lstlisting}[style=python]
============================================================
STEP 1: RAW DATASET INVENTORY
============================================================
  [cancer]     Total files: 481  [ok]
  [no_cancer]  Total files: 506  [ok]
  Total images: 987  [ok]
\end{lstlisting}

\subsection{Normalization}

We applied \textbf{samplewise standard normalization} to every image using Keras's \texttt{ImageDataGenerator}. This involved:
\begin{itemize}
  \item Centering each image by subtracting its mean pixel value.
  \item Standardizing each image by dividing by its standard deviation.
\end{itemize}

Samplewise normalization was chosen for its reliability for relatively small datasets such at this one. As opposed to relying on global statistics (which can be noisy or lead to data leakage), each image is processed independently. This makes the evaluation more reliable and ensures the model handles new images correctly during testing.

\subsection{Data Augmentation}

To help the models learn more robust features and avoid overfitting, we used data augmentation, but \textbf{only for the training set}. We kept the validation and test sets in their original state (except for normalization) to ensure the results reflect real-world performance.

Table~\ref{tab:augmentation} summarizes the augmentations we used. We picked these to simulate the typical variations found in histology slides, such as different rotations and slight scaling differences.

\begin{table}[H]
  \centering
  \caption{Augmentation settings applied to the training set.}
  \label{tab:augmentation}
  \begin{tabular}{lll}
    \toprule
    \rowcolor{tableheader}
    \textbf{Setting} & \textbf{Value} & \textbf{Goal} \\
    \midrule
    \texttt{rotation\_range}    & 30\textdegree  & Handle slides at any angle \\
    \texttt{width/height shift} & 0.15           & Account for slight centering variations \\
    \texttt{shear\_range}       & 0.3            & Simulate tissue distortion \\
    \texttt{zoom\_range}        & 0.30           & Handle magnification differences \\
    \texttt{horizontal\_flip}   & True           & Tissue features are orientation-independent \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Ensuring Experimental Integrity}

A key part of our methodology was ensuring that the data generators were only initialized \textbf{once} at the start of the entire 72-run simulation. If we had recreated the generators inside the training loop, each model would have seen the training images in a different order, adding "luck of the draw" as a hidden factor in our results. By sharing the same generators, we ensured that every architecture and optimizer was tested on the exact same sequence of data.
