\section{Preprocessing}
\label{sec:preprocessing}

% TODO: Describe every preprocessing step applied to the raw images before training:
%   - Image resizing to 256×256 pixels
%   - Pixel normalization (divide by 255 or ImageNet mean/std subtraction)
%   - Data augmentation applied to the training set only:
%       * Horizontal and vertical flips
%       * Random rotation (±20°)
%       * Zoom range (±10%)
%       * Brightness adjustment
%       * Width/height shift
%   - Rationale for each augmentation choice (combat class imbalance / overfitting)
%   - No augmentation applied to val/test sets (only rescaling)
%   - Tools used: TensorFlow ImageDataGenerator or tf.data pipeline
